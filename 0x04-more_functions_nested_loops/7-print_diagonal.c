#include "main.h"

/**
 * print_diagonal - Entry
 * @n: parameter
 * Return: 0
 */
void print_diagonal(int n)
{
	int a;
	int b;

	if (n > 0)
	{
		a = 1;

		while(n > 0)
		{
			_putchar(r);
			n--;
		}
		_putchar(10);
	}
	else
	{
		_putchar(10);
	}
}
